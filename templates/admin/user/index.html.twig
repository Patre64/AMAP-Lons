{% extends "admin/base.html.twig" %}

{% block body %}

</br>
</br>
</br>
</br>

    <h1 style= color:white>Amapiens :</h1>

    <a class="create" href="{{ path('user_new') }}">NOUVEAU</a>

</br>
</br>

    <table class="table table-striped table-dark">
        
        <thead class="thead-light">
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Groupe</th>
                <th>Adresse</th>
                <th>Ville</th>
                <th>CP</th>
                <th>TelFixe</th>
                <th>TelPort1</th>
                <th>TelPort2</th>
                <th>Email</th>
                <th>Actif</th>
                <th>Identifiant</th>
                <th>Password</th>
                <th>CharteAcceptee</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.nom }}</td>
                <td>{{ user.prenom }}</td>
                <td>{{ user.groupe }}</td>
                <td>{{ user.adresse }}</td>
                <td>{{ user.ville }}</td>
                <td>{{ user.CP }}</td>
                <td>{{ user.telFixe }}</td>
                <td>{{ user.telPort1 }}</td>
                <td>{{ user.telPort2 }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.actif ? 'Yes' : 'No' }}</td>
                <td>{{ user.identifiant }}</td>
                <td>{{ user.password }}</td>
                <td>{{ user.charteAcceptee ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('user_edit', {'id': user.id}) }}">Modifier/Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="16">Pas d'enregistrements trouvés !</td>
            </tr>
        {% endfor %}
        </tbody>

    </table>

{% endblock %}
