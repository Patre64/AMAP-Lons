{% extends "admin/base.html.twig" %}

{% block body %}

</br>
</br>
</br>
</br>

    <h1 style= color:white>Distribution :</h1>

    <a class="create" href="{{ path('distribution_new') }}">NOUVEAU</a>

</br>
</br>

    <table class="table table-striped table-dark">

        <thead class="thead-light ">
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>produits</th>
                <th>actions</th>
            </tr>
        </thead>

        <tbody>

        {% for distribution in distributions %}

            <tr>
                <td>{{ distribution.id }}</td>
                <td>{{ distribution.date ? distribution.date|date('Y-m-d') : '' }}</td>
                <td>
                
                    <ul>{% for produit in distribution.produits %}

                    <li>{{ produit.nom }}<li>

                    {% endfor %}</ul></td>

                <td>
                    <a href="{{ path('distribution_edit', {'id': distribution.id}) }}">Modifier/Suprimer</a>
                </td>
            </tr>

        {% else %}
            <tr>
                <td colspan="3">Pas d'enregistrements trouvés !</td>
            </tr>
        {% endfor %}

        </tbody>

    </table>

    {% endblock %}
